# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Estado atual do projeto

Este diretório ainda não contém código-fonte de uma aplicação. Atualmente ele só possui
pastas de configuração de skills/agentes (`.agents/skills/`, `.claude/skills/`) e não é um
repositório git.

Quando este projeto ganhar código real (front-end, back-end, scripts, etc.), atualize este
arquivo com:

- Comandos de build, lint e teste (incluindo como rodar um teste único).
- Arquitetura de alto nível: como as partes do sistema se conectam, principais módulos e
  fluxos de dados.

## Idioma

Responda sempre em português (mensagens, comentários e comunicação com o usuário).
